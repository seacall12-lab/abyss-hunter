[v0.4]

index.html
- 강화, 분해, 자동 분해, 보스 화면 구조가 포함됨
- 장비 상세, 강화 결과, 보스 결과 UI가 포함됨

style.css
- 모바일 세로 화면 중심 레이아웃이 포함됨
- 장비, 강화, 분해, 보스전 UI 스타일이 포함됨

data.js
- gameVersion 0.5.0, saveVersion 5 기준의 데이터 구조를 사용 중
- 장비 등급, 강화 비용/확률, 분해 보상, 보스 데이터가 포함됨

state.js
- saveVersion 5 기준 저장 구조와 마이그레이션이 포함됨
- 장비 장착, 잠금, 강화, 분해, 자동 분해, 보스 진행도 저장이 포함됨

combat.js
- 일반 자동 전투와 보스 전투 런타임이 분리되어 있음
- 보스 스킬, 보스 결과, 보상 지급 흐름이 포함됨

loot.js
- 일반 장비 생성, 보스 장비 보상, 자동 분해 처리가 포함됨
- 장비 점수, 정렬, 필터, 비교 계산이 포함됨

ui.js
- 사냥, 장비, Forge, 보스 화면 이벤트 연결이 포함됨
- 강화, 분해, 자동 분해, 보스 조작 UI가 포함됨

main.js
- 게임 초기화, 루프 업데이트, 자동 저장 흐름이 포함됨
- visibilitychange, pagehide, beforeunload 저장 처리가 포함됨

[v0.5]

data.js
- gameVersion을 0.5.0으로 올리고 saveVersion 5 구조를 사용함

state.js
- 사운드, 진동, 화면 흔들림, 피해 숫자 설정 저장 구조가 추가됨
- 기존 저장에서 v0.5 설정 기본값을 보강하는 마이그레이션이 포함됨

index.html
- 설정 버튼과 설정 하단 시트가 추가됨
- 사냥 화면 상세 능력치/로그 하단 시트와 최근 전투 로그가 추가됨
- Forge 화면이 강화, 분해, 자동 분해 하위 탭 구조로 정리됨

style.css
- 전체 화면 고정형 모바일 레이아웃이 보강됨
- 하단 고정 탭, 하단 시트, Forge 하위 탭, 최근 로그 스타일이 추가됨

ui.js
- Web Audio 기반 효과음, 진동, 화면 흔들림 설정이 연결됨
- 최근 전투 로그와 전체 로그 시트 처리가 추가됨
- Forge 하위 탭 전환과 선택 패널 렌더링이 추가됨
- 일반 사냥 공격, 피격, 치명타, 드롭, 레벨업 Canvas 연출이 추가됨

[현재 상태]
- 현재 gameVersion: 0.5.4
- 현재 saveVersion: 5
- 현재 완료된 버전: v0.5
- 현재 개발 중인 버전: v0.6
- 다음 작업: v0.6 지역 확장 설계와 구현
- 확인이 필요한 문제: 현재 작업 폴더에 .git이 없고 git 명령을 사용할 수 없음; 내부 브라우저 자동화는 Windows sandbox 권한 문제로 확인 필요

[v0.5.2]

data.js
- gameVersion을 0.5.2로 올리고 saveVersion 5를 유지함
- 보스 강타와 방어 쿨다운/지속시간을 소폭 조정해 조작 타이밍을 완화함

index.html
- 정적 파일 캐시 쿼리를 v0.5.2로 갱신함

style.css
- 보스 경고 오버레이 펄스, 방어 활성 테두리, 궁극기 준비 버튼 강조, 쿨다운 마스크 진행률 스타일을 추가함

ui.js
- 보스전 Canvas에 위험 장판, 방어막, 강타 충격파, 궁극기 빔 연출을 추가함
- 보스 스킬별 효과음과 진동 패턴을 분리하고 방어 성공/물약/강타/궁극기 피드백을 보강함

[v0.5.3]

data.js
- gameVersion을 0.5.3으로 올리고 saveVersion 5를 유지함

index.html
- 정적 파일 캐시 쿼리와 문서 제목을 v0.5.3으로 갱신함

style.css
- 하단 탭과 toast가 겹치지 않도록 toast 표시 위치를 상향 조정함
- 360px급 화면에서 사냥 제목 줄바꿈이 어색하지 않도록 전투 제목 레이아웃을 정리함

검증
- JS 문법 검사와 DOM/캐시 계약 검사를 통과함
- Edge headless CDP 기준 360x800, 390x844, 412x915, 1280x800 주요 화면 회귀 검증을 수행함

[v0.5.4]

content-data.js
- data.js에 있던 플레이어, 몬스터, 지역, 보스, 장비, 강화, 스탯 정적 데이터를 분리함

data.js
- 정적 데이터는 GameContentData에서 읽고 계산/빌드 API만 유지하도록 정리함
- saveVersion 5는 유지함

style.css / styles/*.css
- style.css를 CSS 진입점으로 두고 base, views, systems, overlays, mobile 모듈로 분리함

index.html
- content-data.js를 data.js보다 먼저 로드하도록 스크립트 순서를 정리함
- 정적 파일 캐시 쿼리를 v0.5.4로 갱신함
## [v0.5.5] 2026-06-16
- v0.5.4 파일 분리 후 content-data.js 로딩 실패 시 data.js가 fallback으로 복구하도록 안정화.
- 초기 스크립트 오류가 화면에 표시되도록 startup error 표시를 추가해 무반응 원인 확인을 쉽게 함.
